page.status_code #Indica se a página foi baixada com sucesso.
print(soup.prettify()) #Formata o conteúdo da página
soup.find('p') #Encontra a primeira instância de uma tag
soup.find_all()  #Encontra todas as instâncias de uma tag na página
get_text()
soup.find_all(class_=" ") #Acha todos os elementos de uma classe
soup.find_all('X', class_="") #Procura uma tag x que tenha a classe y 
soup.find_all(id="") #Procura todos os elemtos com o id x
soup.select("div p") Utilizando seletores CSS

bs4




